{
  "name": "scalesolo-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server to drive ScaleSolo posting/scheduling (upload -> auto-caption -> schedule with a confirm gate).",
  "bin": { "scalesolo-mcp": "server.js" },
  "scripts": { "start": "node server.js" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "engines": { "node": ">=18" }
}
